name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
        include:
          - os: macos-latest
            rid: osx-arm64
            artifact: build
          - os: windows-latest
            rid: win-x64
            artifact: build.exe
          - os: ubuntu-latest
            rid: linux-x64
            artifact: build
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'
      - run: dotnet restore --nologo -v q
      - run: dotnet test --no-restore --nologo -v q --logger "console;verbosity=minimal"
      - run: dotnet publish src/ThroughlineBuild.Cli -r ${{ matrix.rid }} -c Release --no-restore --nologo -v q
      - uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.rid }}
          path: src/ThroughlineBuild.Cli/bin/Release/net10.0/${{ matrix.rid }}/publish/${{ matrix.artifact }}
