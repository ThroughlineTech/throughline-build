# Architecture document retired

This document was retired because it had drifted from the implementation.

Use the [state-of-the-system index](state-of-the-system/00-index.md) as the
current architecture authority. It maps the system across inventory,
installation, dependencies, configuration, persistence, public surfaces,
contracts, workspace assumptions, failure modes, lifecycle orchestration, and
LLM architecture.

Use the [operator guide](throughline_build_userguide.md) for installation and
day-to-day operation. Source and `build help <topic>` remain authoritative for
the binary currently installed.
