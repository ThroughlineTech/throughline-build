# Checkpoint 01 - After Rule Inventory

- Changed since previous checkpoint: added `.agent-instruction-compaction/rule-inventory.md`; archived scoped instruction and skill files are unchanged.
- Word-count delta from previous checkpoint: 0 for scoped archived files, 6513 -> 6513.
- Normative behavior changed: no. The rule inventory is descriptive and no instruction prose changed.
- Duplicate rules still remaining: root/nested AOT and solution-file repeats; root/nested current-vs-historical docs repeats; skill/reference Bash repeats; `run-backlog/SKILL.md` still carries serial-loop, fan-out, adapter, and worker-contract detail also owned by references.
- Decision: proceed to Step 2, root `AGENTS.md` compaction.
