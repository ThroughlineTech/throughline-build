# Checkpoint 04 - After Skill Router

- Changed since previous checkpoint: `run-backlog/SKILL.md` was shortened into a router and `run-backlog/references/serial-loop.md` was added to preserve the moved serial procedure. Repo instruction files are unchanged.
- Word-count delta from previous checkpoint: 5624 -> 5692, an increase of 68 words because the new serial reference is now archived. `SKILL.md` itself dropped from 1016 -> 757 words and the new `serial-loop.md` is 327 words.
- Normative behavior changed: no. The conductor role, repository/ticket-client authority, Bash contract, mode selection, fan-out preconditions, adaptation validation ban on real tickets, conductor-only ownership, worker mutation bans, unrelated-change preservation, ticket body/comment reading, ticket-client self-change caution, no deploy/push/merge/ship without authorization, run branch bootstrap, deterministic fan-out commands, adapter warning, and final report contents remain present.
- Duplicate rules still remaining: `parallel-fan-out.md` and `repo-adaptation.md` still repeat Bash setup; fan-out/adaptation references still repeat some shared conductor and authorization invariants; `agent-contracts.md` remains intentionally verbose for worker prompts.
- Decision: proceed to Step 5, deduplicate run-backlog references.
